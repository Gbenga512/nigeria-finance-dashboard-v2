name: Legacy report workflow retired
on:
  workflow_dispatch:
jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Legacy report loader retired. The reconciliation statement is now built only by index.html."
