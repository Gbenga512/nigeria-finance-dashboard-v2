name: Legacy print workflow retired
on:
  workflow_dispatch:
jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Legacy print workflow retired. Print styling is now owned by index.html."
