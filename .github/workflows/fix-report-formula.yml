name: Legacy report-formula workflow retired
on:
  workflow_dispatch:
jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Legacy report-formula workflow retired. The report formula is now owned by index.html."
